#!/bin/sh

if [ ! -z "$1" ]; then
  which "$1" > /dev/null || {
    echo "Could not find $1 on path"
    exit 1
  }

  objdump -p "$(which "$1")" | grep NEEDED
  exit 0
fi

exit 1

# vim: set syntax=sh tabstop=2 softtabstop=2 shiftwidth=2 shiftround expandtab:
